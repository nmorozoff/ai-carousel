const Footer = () => (
  <footer className="py-8 border-t border-border">
    <div className="container mx-auto px-4 text-center text-sm text-muted-foreground">
      © {new Date().getFullYear()} Carousel AI. Все права защищены.
    </div>
  </footer>
);

export default Footer;
